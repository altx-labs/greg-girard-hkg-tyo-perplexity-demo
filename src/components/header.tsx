// Header is no longer used as a separate component.
// The header is now inline in page.tsx, matching the Manus design.
// Cart button is part of CartDrawer component (fixed top-right).
// This file is kept to avoid import errors in any other references.

export function Header() {
  return null;
}
